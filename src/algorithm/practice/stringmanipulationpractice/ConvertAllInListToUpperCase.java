/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package algorithm.practice.stringmanipulationpractice;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

/**
 *
 * @author user
 */
public class ConvertAllInListToUpperCase {
    public static void main(String[] args) {
//        List<String> strinList = Arrays.asList("aaa","abc","Acc");
//        List<String> upper = strinList.stream().map(String::toUpperCase).collect(Collectors.toList());
//        System.out.println(upper);
                int x[] = {10,10,10,10,10};
                int sum = Arrays.stream(x).sum();
                System.out.println(sum);
    }
}
